# ── OS / editor ──────────────────────────────────────────────────────────
.DS_Store
*.swp
*.iml
.idea/

# ── Build artifacts ──────────────────────────────────────────────────────
# Each Flutter project keeps its own .gitignore for build/, .dart_tool/, etc.
*.apk
*.aab

# ── OCR pipeline artifacts (huge / regenerable — keep run_ocr.py & raw index)
ocr/venv/
ocr/pages/
ocr/ocr_run.log

# ── Local tooling / agent harness artifacts ──────────────────────────────
.claude/
.agents/
.codex/
.sixth/
.env
